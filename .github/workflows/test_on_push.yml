name: Test on push
# pull_request covers every topic branch, so push is limited to main.
# The previous branch-name prefix list let chore/** and others skip CI entirely.
on:
  pull_request:
  push:
    branches:
      - 'main'
jobs:
  test:
    uses: ./.github/workflows/test.yml
